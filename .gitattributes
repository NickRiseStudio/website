# Все файлы — текст в LF, без автоконвертации.
# Рабочая папка синхронизируется Яндекс.Диском; раньше из-за переводов строк
# git показывал «изменёнными» файлы, которых никто не менял.
* text=auto eol=lf